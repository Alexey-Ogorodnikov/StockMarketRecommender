<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/MainFragment">

    <fragment
        android:id="@+id/MainFragment"
        android:name="com.alexey.stockmarketrecommender.ui.MainFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_main">

        <!--
         Use this template it to go to some others fragments, which is not the case at the moment
        <action
            android:id="@+id/action_MainFragment_to_SomeOtherFragment"
            app:destination="@id/SomeOtherFragment" />
         -->
    </fragment>

</navigation>